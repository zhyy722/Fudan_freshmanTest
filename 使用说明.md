# 复旦大学实验室安全考试 · 考前学习自动挂机阅读

配套脚本：[复旦实验室安全考前学习-自动挂机阅读.user.js](./复旦实验室安全考前学习-自动挂机阅读.user.js)

## 它能做什么

- 自动依次打开列表里所有“未学习”的课件（PDF/视频均可）；
- 每份资料等网页倒计时自然走完后，自动点“阅读完成”提交；
- 翻页也会自动处理（15 条分两页）；
- 全部读完自动停止，不会误点页面上的“学习完成”去开始考试；
- 中途可用面板上的按钮暂停、跳过当前、停止（跳过/停止都会先点“保留阅读时长”，已读时长不丢失）。

## 使用方法（任选一种）

### 方式 1：F12 控制台（最快）

1. 停留在考前学习列表页；
2. 按 `F12` 打开开发者工具，切到 `Console`（控制台）标签；
3. 全选复制脚本文件的全部内容，粘贴进控制台，回车；
4. 右下角会出现“考前学习自动挂机阅读”面板并自动开始。

> 如果控制台提示“粘贴代码有风险”，点“允许粘贴”即可。

### 方式 2：油猴 Tampermonkey（长期复用）

1. 浏览器安装 Tampermonkey 扩展；
2. 点扩展图标 → “添加新脚本”；
3. 删掉默认内容，粘贴脚本文件的全部内容，`Ctrl+S` 保存；
4. 刷新考前学习页，脚本会自动开始。

## 面板按钮

| 按钮 | 作用 |
| --- | --- |
| ▶ 开始 | 开始 / 失败后重新开始 |
| ⏸ 暂停 | 暂停自动操作（打开的弹窗倒计时仍在走） |
| ⏭ 跳过当前 | 保存当前课件的已读时长并跳到下一份 |
| ⏹ 停止 | 停止并保存当前进度 |

## 重要提醒

- 保持本标签页在前台、浏览器窗口不要最小化。页面被隐藏几分钟后，浏览器会大幅降低网页
  计时器速度，倒计时会变得非常慢。
- 每份课件需要等倒计时走完，13 份未学习资料大约需要 2 小时以上，属正常现象。
- 如果登录会话过期，提交会失败，脚本会保存进度并跳过，重新登录后点“开始”可继续。
- 脚本只处理“挂机等待 + 自动点击完成”，不伪造阅读时长；考试/安全内容建议仍要自己了解。
